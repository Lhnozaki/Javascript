const num1 = 100;
const num2 = 50;
const val;

// Simple Math

val = num1 + num2; // 150
val = num1 - num2; // 50
val = num1 * num2; // 5000
val = num1 / num2; // 2
val = num1 % num2; // 0

// Math Objects & Methods

val = Math.PI; // Pi 3.14
val = Math.E; // Eullers Number 2.7182
val = Math.round(2.8); // 3
val = Math.round(2.4); // 2
val = Math.ceil(2.4); // 3 (always rounds up)
val = Math.floor(2.5); // 2 (always rounds down)
val = Math.sqrt(64); // 8 (square root)
val = Math.abs(-3); // 3 (absolute number)
val = Math.pow(8, 2); // 64 (power of)
val = Math.min(2,33,4,1,55,6,3,-2); // -2 (logs minimum numner)
val = Math.max(2,33,4,1,55,6,3,-2); // 55 (logs maximum numner)
val = Math.random(); // Random Number
val = Math.random() * 20; // random number between 0 - 19; including decimals.
val = Math.floor(Math.random() * 20); // random number between 0 - 19. Whole integers only.
